name = "missiai"
pages_build_output_dir = ".vercel/output/static"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

[vars]
# GEMINI_API_KEY is set in Cloudflare Dashboard (Settings > Environment Variables)
# Don't put your actual key here — this file goes to GitHub!

# KV binding is configured via Cloudflare Pages dashboard:
# Settings → Functions → KV namespace bindings → MISSI_MEMORY