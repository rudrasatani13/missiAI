<<<<<<< SEARCH
    const appEnv = getEnv()

    const command = await buildPluginCommand(userMessage, pluginId as PluginId, apiKey)
    const result = await executePluginCommand(command, pluginConfig, apiKey)
=======
    const appEnv = getEnv()

    const command = await buildPluginCommand(userMessage, pluginId as PluginId, appEnv.GEMINI_API_KEY || "")
    const result = await executePluginCommand(command, pluginConfig, appEnv.GEMINI_API_KEY || "")
>>>>>>> REPLACE
